# this file automatically installs the required packages for the project
# to run, simply run the command "python setup.py install" in the terminal
# to uninstall, run the command "pip uninstall <package_name>" in the terminal
# to install a new package, run the command "pip install <package_name>" in the terminal


# FINISH THIS FILE